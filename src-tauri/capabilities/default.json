{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop permissions for Personal CRM",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
